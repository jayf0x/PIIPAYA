#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"
PYTHON_BIN="$REPO_ROOT/src-python/.venv/bin/python"
ENTRYPOINT="$REPO_ROOT/src-python/__main__.py"

exec "$PYTHON_BIN" "$ENTRYPOINT"
